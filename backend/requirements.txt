# ============================================================================
# EchoScene — Unified Backend & AI Pipeline Requirements
# ============================================================================

# ── Web Framework & API ─────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
slowapi==0.1.9
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
httpx==0.27.2

# ── Database & Storage ──────────────────────────────────────────────────────
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
aiosqlite==0.20.0
minio==7.2.12
python-dotenv==1.0.1

# ── Ollama Client (Vision Models) ───────────────────────────────────────────
ollama==0.3.0               # Python client for Ollama server (qwen2.5-vl:3b)
requests==2.31.0            # HTTP requests for Ollama API endpoints

# ── AWS & Retry Mechanisms ──────────────────────────────────────────────────
boto3==1.34.131             # AWS SDK for Bedrock
tenacity==8.3.0             # Resilient retry strategies

# ── Video Processing & Computer Vision ──────────────────────────────────────
opencv-contrib-python-headless==4.10.0.84
Pillow==10.4.0
scenedetect[opencv]==0.6.4
ffmpeg-python==0.2.0
pymediainfo==6.1.0
python-magic==0.4.27

# ── Speech-to-Text (ASR) ───────────────────────────────────────────────────
faster-whisper==1.0.3       # faster-whisper: ctranslate2 backend, no torch dependency
librosa==0.10.1
soundfile==0.12.1

# ── Vector Search & Embeddings ────────────────────────────────────────────
chromadb-client==1.5.9
scikit-learn==1.5.1

# ── Data Processing ───────────────────────────────────────────────────────
numpy==1.26.4               # numba (dep của whisper) yêu cầu numpy<2.0
scipy==1.14.1
pandas==2.2.2

# ── Testing & Code Quality ────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==5.0.0
pytest-timeout==2.1.0
faker==28.0.0
ruff==0.8.4
mypy==1.14.0
types-PyYAML==6.0.12.20240917

# ── Utilities ──────────────────────────────────────────────────────────────
python-json-logger==2.0.7
python-dateutil==2.9.0.post0

# ── Real-time & Message Broker ─────────────────────────────────────────────
redis==5.2.0

# ── Vector DB ──────────────────────────────────────────────────────────────
pgvector==0.3.6